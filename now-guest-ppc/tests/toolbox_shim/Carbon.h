#ifndef NOW_TEST_CARBON_H
#define NOW_TEST_CARBON_H

typedef unsigned char Boolean;
typedef int OSErr;
typedef struct Rect {
    short top, left, bottom, right;
} Rect;

enum { noErr = 0, paramErr = -50 };

#endif
