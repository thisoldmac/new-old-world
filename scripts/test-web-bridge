#!/bin/sh
# NOW-owned host-side browser bridge. stdlib-only; boots no browser or guest.
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)

PYTHONDONTWRITEBYTECODE=1 \
    python3 -m unittest discover -s "$repo_root/web-bridge/tests" -v

echo "==> Web Bridge gate passed"

