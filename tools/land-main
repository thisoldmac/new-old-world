#!/bin/sh
# Kept as a refusal so old scripts fail with the new workflow rather than
# silently writing local main. GitHub is now the only landing boundary.
echo "[land-main] retired: main accepts protected GitHub pull requests only." >&2
echo "  Push the feature branch; after GitHub merges it, run tools/sync-main." >&2
exit 1
