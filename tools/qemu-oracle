#!/usr/bin/env python3
"""Read classic Mac state from one explicitly identified QEMU development VM."""

from pathlib import Path
import sys


HERE = Path(__file__).resolve().parent
sys.path.insert(0, str(HERE))

from qemu_oracle.cli import main


if __name__ == "__main__":
    raise SystemExit(main())
