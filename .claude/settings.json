{
  "$comment": "Two enforced loops. The Mirror drive loop (tools/mirror-gate) and the Toolbox/GWorld research arc (tools/toolbox-re-gate); each is a no-op unless its own state file exists, so a session running neither pays nothing. See docs/mirror-drive-loop.md and docs/local/toolbox-re/PLAN.md.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR/tools/mirror-gate\" check",
            "timeout": 15,
            "statusMessage": "mirror-gate: is the sweep finished?"
          }
        ]
      },
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR/tools/toolbox-re-gate\" check",
            "timeout": 15,
            "statusMessage": "toolbox-re-gate: is the research arc finished?"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR/tools/mirror-gate\" guard-bash",
            "timeout": 15,
            "statusMessage": "mirror-gate: checking this is the mirror"
          }
        ]
      }
    ]
  }
}
