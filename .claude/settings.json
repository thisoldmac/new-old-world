{
  "$comment": "The Mirror drive loop, enforced rather than remembered. Both hooks are no-ops unless docs/local/mirror-sweep-state.json exists, so they cost a non-Mirror session nothing. See tools/mirror-gate and docs/mirror-drive-loop.md.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR/tools/mirror-gate\" check",
            "timeout": 15,
            "statusMessage": "mirror-gate: is the sweep finished?"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR/tools/mirror-gate\" guard-bash",
            "timeout": 15,
            "statusMessage": "mirror-gate: checking this is the mirror"
          }
        ]
      }
    ]
  }
}
