cmake_minimum_required(VERSION 3.13)
project(NowCensusTrapSpike C)

# Throwaway: answers one question and is then deleted. See README.md.
add_application(now-trap-spike
    CREATOR "NOWs"
    main.c
)
